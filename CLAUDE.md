# flowstep

> Declarative workflow engine for Go. See `.claude/rules/` for detailed project rules.

## Core Rules

- NEVER mention `co-authored-by` or the tool used to create commits/PRs.
- All timestamps stored in **UTC** (`TIMESTAMPTZ`). Projected to facility timezone at display only.

## Rules

- @.claude/rules/flowstep-project.md — Tech stack, directory structure, dev commands
- @.claude/rules/flowstep-architecture.md — Package layout, coding standards, forbidden patterns, testing strategy
- @.claude/rules/flowstep-versioning.md — SemVer, CHANGELOG, documentation trail
